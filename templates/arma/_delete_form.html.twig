<form method="post" action="{{ path('app_arma_delete', {'id': arma.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ arma.id) }}">
    <button class="btn">Delete</button>
</form>
