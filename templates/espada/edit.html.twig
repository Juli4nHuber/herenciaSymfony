{% extends 'base.html.twig' %}

{% block title %}Edit Espada{% endblock %}

{% block body %}
    <h1>Edit Espada</h1>

    {{ include('espada/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_espada_index') }}">back to list</a>

    {{ include('espada/_delete_form.html.twig') }}
{% endblock %}
