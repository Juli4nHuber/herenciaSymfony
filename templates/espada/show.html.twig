{% extends 'base.html.twig' %}

{% block title %}Espada{% endblock %}

{% block body %}
    <h1>Espada</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ espada.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ espada.nombre }}</td>
            </tr>
            <tr>
                <th>Durabilidad</th>
                <td>{{ espada.durabilidad }}</td>
            </tr>
            <tr>
                <th>Critico</th>
                <td>{{ espada.critico }}</td>
            </tr>
            <tr>
                <th>Letalidad</th>
                <td>{{ espada.letalidad }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_espada_index') }}">back to list</a>

    <a href="{{ path('app_espada_edit', {'id': espada.id}) }}">edit</a>

    {{ include('espada/_delete_form.html.twig') }}
{% endblock %}
