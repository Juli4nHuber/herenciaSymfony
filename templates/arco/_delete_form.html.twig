<form method="post" action="{{ path('app_arco_delete', {'id': arco.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ arco.id) }}">
    <button class="btn">Delete</button>
</form>
