{% extends 'base.html.twig' %}

{% block title %}Arco{% endblock %}

{% block body %}
    <h1>Arco</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ arco.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ arco.nombre }}</td>
            </tr>
            <tr>
                <th>Durabilidad</th>
                <td>{{ arco.durabilidad }}</td>
            </tr>
            <tr>
                <th>CantFlechas</th>
                <td>{{ arco.cantFlechas }}</td>
            </tr>
            <tr>
                <th>Rango</th>
                <td>{{ arco.rango }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_arco_index') }}">back to list</a>

    <a href="{{ path('app_arco_edit', {'id': arco.id}) }}">edit</a>

    {{ include('arco/_delete_form.html.twig') }}
{% endblock %}
